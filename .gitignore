# Raw pulls (keep local; publish canonical outputs instead)
odds/raw/

# ncaahoopR data is too large for GitHub (6.7 GB) - sync separately
ncaahoopR_data-master/

# Large normalized files (>100MB) - sync to Azure instead
odds/normalized/odds_team_rows_canonical.csv
odds/normalized/odds_canonical_matchups.csv

# Temp/working files
*.tmp
*.bak
